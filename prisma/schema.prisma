datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Post {
  id          String @unique @default(uuid())
  slug        String @unique
  views       Int    @default(0)
  title       String
  date        String
  description String
}
